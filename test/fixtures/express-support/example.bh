var BH = require('../../../lib/bh');
var bh = new BH();

bh.match('root', function (ctx) {
    ctx
        .tag('example')
        .attr('name', 'value')
        .content(ctx.json().data.name);
});

module.exports = bh;
